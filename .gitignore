# ── OS 生成文件 ──
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── 编辑器 / IDE ──
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# ── 日志 ──
*.log
npm-debug.log*

# ── 依赖（如未来引入构建工具）──
node_modules/
dist/
.cache/

# ── 环境变量（不要提交 API Key）──
.env
.env.local
.env.production

# ── 临时文件 ──
*.tmp
*.bak
*.swp

# ── 自定义文件 ──
*.zip
config.txt
git操作方式.txt
SEO.txt
*.py
